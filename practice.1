
include<stdio.h>
#include<math.h>

int main(){
	double t1, t2, a = sqrt(2);
	t2 = 2;

	do {
		t1 = t2;
		t2 = t1 * 2 / a;
		a = sqrt(2 + a);
	} while (fabs(t2 - t1) >= 1e-6);

	printf("pi=%.2f\n", t1);

	return 0;
}



#include<stdio.h>
#include<math.h>

int main(){
	double t1 = 0, t2, a = 2;
	t2 = 4.0 / 3;
	while (fabs(t2-t1)>=1e-6)
	{
		t1 = t2;
		t2 = t1 * ((2 * a * 2 * a) * 1.0) / ((2 * a - 1) * (2 * a + 1));
		a++;
	}
	
	printf("pi=%7.2f\n", 2*t1);

	return 0;
}

#include<stdio.h>
#include<math.h>

int main(){
	double t1 = 0, t2 = 0, a1 = 1,a2 = 1,b,c;
	t2 = a1 * 1.0 / a2;
	do {
		b = a1 + a2;
		a1 = a2;
		a2 = b;
		t1 = t2;
		t2 = a1 * 1.0 / a2;
	} while (fabs(t1 - t2) >= 1e-5);
	/*while (fabs(t1-t2)>=1e-5)
	{
		
		b = a1 + a2;
		a1 = a2;
		a2 = b;
		t1 = t2;
		t2 = a1 * 1.0 / a2;
	}*/
	
	
	printf("常数=%.3f\n", t1);

	return 0;
}
